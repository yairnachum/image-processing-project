# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Data & models (large, not for git) — tracked dataset samples can be added explicitly
data/raw/
data/clean/
data/distorted/
data/restored/
data/finetune/
data/processed/
weights/

# Per-tile model artefacts for distorted and restored stages (~230 MB each
# of HED PNGs + ORB npz files + YOLO txt files). The per-combo summary CSVs
# and sweep aggregates are still tracked under results/.
outputs/distorted/
outputs/restored/
*.pt
*.pth
*.onnx
*.h5
*.ckpt
runs/

# Logs
*.log
wandb/

# Env
.env

# Course brief (kept locally, not tracked)
3002_CousreProject.pdf

# Design / spec docs (kept locally, not tracked)
docs/
